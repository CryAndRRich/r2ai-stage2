# Deps cho pipeline chạy local (CPU-only). Pin version để khớp với môi trường Kaggle
# (ARCHITECTURE.md mục 2: re-execute pandas_query ở local phải cùng version pandas/numpy).
lxml==5.3.0
pandas==2.2.3
numpy==2.1.3
bm25s==0.2.6
underthesea==6.8.4
pydantic==2.9.2
PyYAML==6.0.2
pytest==8.3.3
